import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Disable development-mode UI completely as requested
  devIndicators: false,
}

export default nextConfig
